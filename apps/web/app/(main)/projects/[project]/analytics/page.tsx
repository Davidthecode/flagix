import PageClient from "./page-client";

export const metadata = {
  title: "Analytics",
  description: "Manage your project analytics",
};

export default function page() {
  return <PageClient />;
}
