import PageClient from "./page-client";

export const metadata = {
  title: "Environments",
  description: "Manage your project environments",
};

export default function page() {
  return <PageClient />;
}
